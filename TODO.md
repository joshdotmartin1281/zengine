Change from glad to zlg.
Change the branch to dev
Break up the files of window_manager and input_manager into smaller files with individual actions
Change the json mapping of an action to just "w" action: "forward" or something like that to allow for easier to read json
add an input context to expect different states of a person.
Add a git commit file to allow for better commit messages.
